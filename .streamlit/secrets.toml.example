# Streamlit secrets for Store Visit Analyzer
# Copy this file to .streamlit/secrets.toml and replace the placeholder.
# NEVER commit secrets.toml — it is excluded via .gitignore.

ANTHROPIC_API_KEY = "your-key-here"
